# OpenAI API Configuration
OPENAI_API_KEY=sk-your-api-key-here

# Optional: Custom OpenAI-compatible endpoint
# OPENAI_BASE_URL=https://your-api.com/v1

# Optional: Default model (gpt-4o-mini is cheapest, gpt-4 for best quality)
# OPENAI_MODEL=gpt-4o-mini

# Optional: Context window size for automatic message trimming
# When set, old conversation history is trimmed to fit within this token limit
# CONTEXT_WINDOW=128000
